# `src/utils`

**Purpose.** Cross-cutting helpers shared by every other subpackage.

Planned responsibilities (not yet implemented):

- Structured logging setup.
- Config loading / merging (YAML + CLI overrides).
- Filesystem / IO helpers.
- Seed control and reproducibility utilities.
- Device selection (MPS / CUDA / CPU).
