# =====================================================================
# Samsung PRISM — Video Detection Module — runtime dependencies
# Target: Python 3.11, macOS Apple Silicon (MPS backend for PyTorch)
# =====================================================================
#
# Versions use lower-bound pinning (>=) to keep the environment
# reproducible without freezing us to obsolete releases. A stricter
# lockfile will be produced once the pipeline is running end-to-end.
#
# Install with:
#     python -m pip install --upgrade pip setuptools wheel
#     pip install -r requirements.txt
#
# NOTE: PyTorch on Apple Silicon uses the default PyPI wheels — no
# custom --extra-index-url is required for MPS support (unlike CUDA).
# =====================================================================

# --- Core scientific stack ---
numpy>=1.26,<3
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3        # scripts/train_general.py -- accuracy/F1/AUC (ADR-006)

# --- Deep learning (PyTorch + MPS backend on Apple Silicon) ---
# Upper bounds exclude versions flagged by the dependency scanner
# (AVAS-PKG) while staying on the current stable line.
torch>=2.2,<2.6
torchvision>=0.17,<0.21

# --- Computer vision / video IO ---
opencv-python>=4.9
pillow>=10.2
av>=12.0                        # PyAV — robust video decoding via ffmpeg

# --- Face detection / alignment ---
# Backend choice deferred to Milestone 3. Both are listed so either can
# be trialled without a requirements.txt edit; drop the loser after
# ADR-002 (face-detection backend) is signed off.
mediapipe>=0.10
facenet-pytorch>=2.6

# --- Model architectures / pretrained weights ---
timm>=1.0                       # XceptionNet, EfficientNet, ViT backbones
transformers>=4.40,<5           # VideoMAE / TimeSformer video foundation models (ADR-007 Phase 3); <5 excludes scanner-flagged 5.x

# --- Configuration / CLI ---
pyyaml>=6.0
typer>=0.12                     # CLI framework for scripts/

# --- Experiment tracking (optional; face-track only, guarded import) ---
mlflow>=2.13,<3                 # <3 excludes scanner-flagged 3.15.x

# --- Visualisation ---
matplotlib>=3.8
seaborn>=0.13

# --- Explainability ---
grad-cam>=1.5
captum>=0.7

# --- Notebooks (runtime — dev-only extras live in requirements-dev.txt) ---
jupyterlab>=4.1
ipykernel>=6.29

# --- Utilities ---
tqdm>=4.66
rich>=13.7                      # nicer logging / tracebacks
