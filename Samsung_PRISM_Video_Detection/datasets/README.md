# `datasets/`

This directory holds **dataset registration manifests** — small JSON /
YAML files that describe where each dataset lives, its version, its
license, and how it maps into `data/raw/`.

The datasets themselves are **not committed to the repository** (see
`.gitignore`). Only the manifests are tracked.

No manifests exist yet.
