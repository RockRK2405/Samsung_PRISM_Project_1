"""Evaluate a trained detector on one or more datasets.

Milestone: 8 — Cross-dataset evaluation & robustness testing.

Planned behaviour:
    - Load a checkpoint from ``checkpoints/``.
    - Run inference over the requested split(s).
    - Compute F1, precision, recall, AUC, FPR — overall and per manipulation type.
    - Write reports to ``outputs/metrics/`` and plots to ``outputs/visualizations/``.

Nothing is implemented yet.
"""

# TODO(milestone-8): implement evaluation harness driven by src/evaluation.


def main() -> None:
    """Entry point — not yet implemented."""
    raise NotImplementedError("scripts/evaluate.py is a placeholder.")


if __name__ == "__main__":
    main()
