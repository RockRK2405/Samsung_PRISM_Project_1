# `tests/`

Placeholder test package. Real tests will be added milestone by milestone,
mirroring the structure of `src/` (e.g. `tests/preprocessing/`,
`tests/models/`, ...). Pytest is the intended runner.

Nothing is implemented yet.
